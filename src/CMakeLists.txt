SET_SOURCE_FILES_PROPERTIES(greet.i PROPERTIES CPLUSPLUS ON)
SET_SOURCE_FILES_PROPERTIES(greet.i PROPERTIES SWIG_FLAGS "-includeall")
SWIG_ADD_LIBRARY(greet LANGUAGE python SOURCES greet.i greet.cpp)
SWIG_LINK_LIBRARIES(greet ${PYTHON_LIBRARIES})

